export const config = {
  /** Number of questions shown per module */
  questionsPerModule: 9,

  /** Minimum correct answer ratio to pass a module (0–1) */
  passThreshold: 0.7,

  /** localStorage key used to persist progress */
  storageKey: "archpull-progress",
};
